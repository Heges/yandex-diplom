---
apiVersion: v2
type: application
name: momo-store
description: Momo Chart

version: "${VERSION}"
appVersion: "latest"

dependencies:
  - name: backend
    version: "${VERSION}"
  - name: frontend
    version: "${VERSION}"
  - name: ingress-nginx
    version: 4.12.0
    repository: https://kubernetes.github.io/ingress-nginx
  - name: ingress
    version: "${VERSION}"

